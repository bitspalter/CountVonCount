//////////////////////////////////////////////////////////////////////////////////
// [ C_DataSet_Class_Source ]
//////////////////////////////////////////////////////////////////////////////////

#include "C_Dataset.hpp"

////////////////////////////////////////////////////////////////////////////////////////////
// [ read_file ]
////////////////////////////////////////////////////////////////////////////////////////////
u_char* C_Dataset::read_file(const char* szFile){
  
   std::ifstream file(szFile, std::ios::binary | std::ios::ate | std::ios::in);

   if(!file.is_open()) return(nullptr);

   std::streamsize size = file.tellg();
   file.seekg(0, std::ios::beg);

   if(size <= 0) return(nullptr);

   u_char* buffer = new u_char[size];
   file.read((char*)buffer, size);
   
   return(buffer);
}